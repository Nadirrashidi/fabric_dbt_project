
# yaml-language-server: $schema=none

name: 'fabric_project'
version: '1.0.0'

profile: 'fabric_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fabric_project:
    bronze:
      +materialized: table
seeds:
  fabric_project:      
    +schema: bronze