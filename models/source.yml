version: 2

sources:
  - name: Source
    database: "{{ 'wh_nadir_project_Prod' if target.name == 'prod' else 'wh_nadir_project' }}"
    schema: Source
    tables:
      - name: dim_customer
      - name: dim_product
      - name: dim_date
      - name: dim_store
      - name: fact_returns
      - name: fact_sales